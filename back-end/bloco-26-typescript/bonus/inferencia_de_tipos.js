var spaceship = {
    name: 'string',
    speed: 0 // att o tipo number sem dizer que é number
};
function accelerate(spaceshipt, speed) {
    spaceship.speed = speed;
}
;
accelerate(spaceship, 50);
